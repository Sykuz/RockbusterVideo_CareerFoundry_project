Find the average, maximum, and minimum rental_duration by rating.

SELECT rating, MAX(rental_duration) AS max_rental_duration
FROM film
GROUP BY rating;

SELECT rating, MIN(rental_duration) AS min_rental_duration
FROM film
GROUP BY rating;

SELECT rating, AVG(rental_duration) AS avg_rental_duration
FROM film
GROUP BY rating;
