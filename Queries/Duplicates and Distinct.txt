Find a Duplicate movie
SELECT title, release_year, language_id, rental_duration, COUNT(*)
FROM film
GROUP BY title, release_year, language_id, rental_duration
HAVING COUNT(*) > 1;

Distinct
SELECT DISTINCT title, release_year, language_id, rental_duration
FROM film;
