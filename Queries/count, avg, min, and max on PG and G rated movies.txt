In the list with film ratings of PG and G find the count of the movies, average rental rate, and maximum and minimum rental durations.

  SELECT
  COUNT(film_id) AS count_of_movies,
  AVG(rental_rate) AS avg_rental_rate,
  MAX(rental_duration) AS max_rental_duration,
  MIN(rental_duration) AS min_rental_duration
  FROM film
  WHERE rating IN ('PG', 'G');

Grouped by rating
  SELECT
  COUNT(film_id) AS count_of_movies,
  AVG(rental_rate) AS avg_rental_rate,
  MAX(rental_duration) AS max_rental_duration,
  MIN(rental_duration) AS min_rental_duration
  FROM film
  WHERE rating IN ('PG', 'G')
  GROUP BY rating;
